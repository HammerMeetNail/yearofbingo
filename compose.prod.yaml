# Run the production image from quay.io locally
# Usage: podman compose -f compose.prod.yaml up
#
# This uses the same PostgreSQL and Redis setup as development,
# but pulls the production container image instead of building locally.

services:
  app:
    image: quay.io/yearofbingo/yearofbingo:latest
    ports:
      - "8080:8080"
    environment:
      - SERVER_HOST=0.0.0.0
      - SERVER_PORT=8080
      - APP_ENV=production
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=bingo
      - DB_PASSWORD=bingo_dev_password
      - DB_NAME=nye_bingo
      - DB_SSLMODE=disable
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=
      - REDIS_DB=0
      # Billing (Stripe)
      - BILLING_ENABLED=${BILLING_ENABLED:-false}
      - FEATURE_TEMPLATES_ENABLED=${FEATURE_TEMPLATES_ENABLED:-true}
      - FEATURE_EDIT_AFTER_FINALIZE_ENABLED=${FEATURE_EDIT_AFTER_FINALIZE_ENABLED:-true}
      - FEATURE_AI_ENABLED=${FEATURE_AI_ENABLED:-false}
      - FEATURE_AI_ENHANCEMENTS_ENABLED=${FEATURE_AI_ENHANCEMENTS_ENABLED:-true}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
      - STRIPE_API_BASE_URL=${STRIPE_API_BASE_URL}
      - STRIPE_PREMIUM_PRICE_MONTHLY=${STRIPE_PREMIUM_PRICE_MONTHLY}
      - STRIPE_PREMIUM_PRICE_YEARLY=${STRIPE_PREMIUM_PRICE_YEARLY}
      - STRIPE_PREMIUM_PRICE_LIFETIME=${STRIPE_PREMIUM_PRICE_LIFETIME}
      - STRIPE_TIP_PRICE_5=${STRIPE_TIP_PRICE_5}
      - STRIPE_TIP_PRICE_10=${STRIPE_TIP_PRICE_10}
      - STRIPE_TIP_PRICE_20=${STRIPE_TIP_PRICE_20}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - AI_PREMIUM_ENHANCEMENTS_PER_MONTH=${AI_PREMIUM_ENHANCEMENTS_PER_MONTH:-100}
      - AI_PREMIUM_ENDPOINT_RATE_LIMIT=${AI_PREMIUM_ENDPOINT_RATE_LIMIT:-60}
      - GOOGLE_OAUTH_ENABLED=${GOOGLE_OAUTH_ENABLED:-false}
      - GOOGLE_OAUTH_CLIENT_ID=${GOOGLE_OAUTH_CLIENT_ID}
      - GOOGLE_OAUTH_CLIENT_SECRET=${GOOGLE_OAUTH_CLIENT_SECRET}
      - GOOGLE_OAUTH_REDIRECT_URL=${GOOGLE_OAUTH_REDIRECT_URL}
      - GOOGLE_OIDC_ISSUER_URL=${GOOGLE_OIDC_ISSUER_URL:-https://accounts.google.com}
      - GOOGLE_OIDC_SCOPES=${GOOGLE_OIDC_SCOPES:-openid,email,profile}
      - OAUTH_ALLOWED_PROVIDERS=${OAUTH_ALLOWED_PROVIDERS:-google}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  postgres:
    image: docker.io/library/postgres:16-alpine
    environment:
      - POSTGRES_USER=bingo
      - POSTGRES_PASSWORD=bingo_dev_password
      - POSTGRES_DB=nye_bingo
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bingo -d nye_bingo"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: docker.io/library/redis:7-alpine
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
