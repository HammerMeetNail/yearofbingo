services:
  app:
    image: localhost/yearofbingo_app:latest
    build:
      context: .
      dockerfile: Containerfile
    ports:
      - "8080:8080"
    networks:
      default:
        aliases:
          - app.test
    environment:
      - SERVER_HOST=0.0.0.0
      - SERVER_PORT=8080
      - APP_ENV=development
      - DEBUG=true
      - DEBUG_LOG_MAX_CHARS=8000
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=bingo
      - DB_PASSWORD=bingo_dev_password
      - DB_NAME=nye_bingo
      - DB_SSLMODE=disable
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=
      - REDIS_DB=0
      # Email configuration for local development
      - EMAIL_PROVIDER=smtp
      - SMTP_HOST=mailpit
      - SMTP_PORT=1025
      - EMAIL_FROM_ADDRESS=noreply@yearofbingo.com
      - EMAIL_FROM_NAME=Year of Bingo
      - APP_BASE_URL=${APP_BASE_URL:-http://localhost:8080}
      - E2E_DEBUG_STRIPE_SIG=${E2E_DEBUG_STRIPE_SIG}
      # Billing (Stripe)
      - BILLING_ENABLED=${BILLING_ENABLED:-false}
      - FEATURE_TEMPLATES_ENABLED=${FEATURE_TEMPLATES_ENABLED:-true}
      - FEATURE_EDIT_AFTER_FINALIZE_ENABLED=${FEATURE_EDIT_AFTER_FINALIZE_ENABLED:-true}
      - FEATURE_AI_ENABLED=${FEATURE_AI_ENABLED:-false}
      - FEATURE_AI_ENHANCEMENTS_ENABLED=${FEATURE_AI_ENHANCEMENTS_ENABLED:-true}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
      - STRIPE_API_BASE_URL=${STRIPE_API_BASE_URL}
      - STRIPE_PREMIUM_PRICE_MONTHLY=${STRIPE_PREMIUM_PRICE_MONTHLY}
      - STRIPE_PREMIUM_PRICE_YEARLY=${STRIPE_PREMIUM_PRICE_YEARLY}
      - STRIPE_PREMIUM_PRICE_LIFETIME=${STRIPE_PREMIUM_PRICE_LIFETIME}
      - STRIPE_TIP_PRICE_5=${STRIPE_TIP_PRICE_5}
      - STRIPE_TIP_PRICE_10=${STRIPE_TIP_PRICE_10}
      - STRIPE_TIP_PRICE_20=${STRIPE_TIP_PRICE_20}
      - AI_PREMIUM_ENHANCEMENTS_PER_MONTH=${AI_PREMIUM_ENHANCEMENTS_PER_MONTH:-100}
      - AI_PREMIUM_ENDPOINT_RATE_LIMIT=${AI_PREMIUM_ENDPOINT_RATE_LIMIT:-60}
      - REMINDERS_POLL_INTERVAL=${REMINDERS_POLL_INTERVAL:-1m}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - GEMINI_MODEL=${GEMINI_MODEL}
      - GEMINI_THINKING_LEVEL=${GEMINI_THINKING_LEVEL:-minimal}
      - GEMINI_THINKING_BUDGET=${GEMINI_THINKING_BUDGET}
      - GEMINI_TEMPERATURE=${GEMINI_TEMPERATURE}
      - GEMINI_MAX_OUTPUT_TOKENS=${GEMINI_MAX_OUTPUT_TOKENS}
      - AI_STUB=${AI_STUB}
      - GOOGLE_OAUTH_ENABLED=${GOOGLE_OAUTH_ENABLED:-false}
      - GOOGLE_OAUTH_CLIENT_ID=${GOOGLE_OAUTH_CLIENT_ID}
      - GOOGLE_OAUTH_CLIENT_SECRET=${GOOGLE_OAUTH_CLIENT_SECRET}
      - GOOGLE_OAUTH_REDIRECT_URL=${GOOGLE_OAUTH_REDIRECT_URL}
      - GOOGLE_OIDC_ISSUER_URL=${GOOGLE_OIDC_ISSUER_URL:-https://accounts.google.com}
      - GOOGLE_OIDC_SCOPES=${GOOGLE_OIDC_SCOPES:-openid,email,profile}
      - OAUTH_ALLOWED_PROVIDERS=${OAUTH_ALLOWED_PROVIDERS:-google}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      mailpit:
        condition: service_started
    volumes:
      - ./web:/app/web:ro
    develop:
      watch:
        - action: rebuild
          path: .
          ignore:
            - web/
        - action: sync
          path: ./web
          target: /app/web

  playwright:
    image: localhost/yearofbingo_playwright:latest
    build:
      context: .
      dockerfile: Containerfile.playwright
    profiles:
      - e2e
    depends_on:
      app:
        condition: service_healthy
    environment:
      - PLAYWRIGHT_BASE_URL=${PLAYWRIGHT_BASE_URL:-http://app.test:8080}
      - PLAYWRIGHT_HEADLESS=${PLAYWRIGHT_HEADLESS:-true}
      - PLAYWRIGHT_BROWSERS=${PLAYWRIGHT_BROWSERS:-firefox}
      - PLAYWRIGHT_WORKERS=${PLAYWRIGHT_WORKERS:-}
      - PWDEBUG=${PWDEBUG:-}
      - PLAYWRIGHT_OUTPUT_DIR=${PLAYWRIGHT_OUTPUT_DIR:-/test-results}
      - PLAYWRIGHT_REPORT_DIR=${PLAYWRIGHT_REPORT_DIR:-/playwright-report}
      - FEATURE_AI_ENABLED=${FEATURE_AI_ENABLED:-false}
      - OIDC_BASE_URL=${OIDC_BASE_URL:-http://oidc:5555}
      # Stripe/billing test env vars
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET:-}
      - STRIPE_MOCK_BASE_URL=${STRIPE_MOCK_BASE_URL:-http://stripe-mock:12111}
      - STRIPE_PREMIUM_PRICE_MONTHLY=${STRIPE_PREMIUM_PRICE_MONTHLY:-price_premium_monthly}
      - STRIPE_PREMIUM_PRICE_YEARLY=${STRIPE_PREMIUM_PRICE_YEARLY:-price_premium_yearly}
      - STRIPE_PREMIUM_PRICE_LIFETIME=${STRIPE_PREMIUM_PRICE_LIFETIME:-price_premium_lifetime}
      - STRIPE_TIP_PRICE_5=${STRIPE_TIP_PRICE_5:-price_tip_5}
      - STRIPE_TIP_PRICE_10=${STRIPE_TIP_PRICE_10:-price_tip_10}
      - STRIPE_TIP_PRICE_20=${STRIPE_TIP_PRICE_20:-price_tip_20}
    volumes:
      - ./:/app:ro
      - ./test-results:/test-results
      - ./playwright-report:/playwright-report
    working_dir: /app
    shm_size: "1gb"

  postgres:
    image: docker.io/library/postgres:16-alpine
    environment:
      - POSTGRES_USER=bingo
      - POSTGRES_PASSWORD=bingo_dev_password
      - POSTGRES_DB=nye_bingo
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bingo -d nye_bingo"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: docker.io/library/redis:7-alpine
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  mailpit:
    image: docker.io/axllent/mailpit:latest
    ports:
      - "127.0.0.1:8025:8025"  # Web UI
      - "127.0.0.1:1025:1025"  # SMTP
    environment:
      - MP_SMTP_AUTH_ACCEPT_ANY=1
      - MP_SMTP_AUTH_ALLOW_INSECURE=1

  oidc:
    image: localhost/yearofbingo_oidc:latest
    build:
      context: .
      dockerfile: Containerfile.oidc
    profiles:
      - e2e
    environment:
      - OIDC_ISSUER_URL=${OIDC_ISSUER_URL:-http://oidc:5555}
      - OIDC_CLIENT_ID=${OIDC_CLIENT_ID:-oidc-test}
      - OIDC_CLIENT_SECRET=${OIDC_CLIENT_SECRET:-oidc-secret}
      - OIDC_REDIRECT_URI=${OIDC_REDIRECT_URI:-http://app.test:8080/api/auth/google/callback}
    expose:
      - "5555"
    ports:
      - "127.0.0.1:5555:5555"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5555/.well-known/openid-configuration"]
      interval: 5s
      timeout: 3s
      retries: 5

  stripe-mock:
    image: localhost/yearofbingo_stripe_mock:latest
    build:
      context: .
      dockerfile: Containerfile.stripe-mock
    environment:
      - STRIPE_MOCK_PUBLIC_BASE_URL=${STRIPE_MOCK_PUBLIC_BASE_URL}
    profiles:
      - e2e
    expose:
      - "12111"
    ports:
      - "127.0.0.1:12111:12111"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:12111/health"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  postgres_data:
  redis_data:
