name: CI

on:
  push:
    branches: [main]
    tags:
      - 'v*'  # Trigger on version tags (v1.0.0, v1.2.3, etc.)
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      deploy_only:
        description: 'Skip build, deploy existing image'
        type: boolean
        default: false
      coverage_only:
        description: 'Run only Go coverage upload'
        type: boolean
        default: false

env:
  GO_VERSION: "1.25"
  REGISTRY: quay.io
  IMAGE_NAME: yearofbingo/yearofbingo

jobs:
  badge:
    name: Badge (Main Push)
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: No-op for badge status
        run: echo "Badge update run on main."

  coverage-main:
    name: Coverage Upload (Main)
    runs-on: ubuntu-latest
    if: >
      (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'workflow_dispatch' && inputs.coverage_only)
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Download dependencies
        run: go mod download

      - name: Run tests with coverage
        run: go test -v -race -coverprofile=coverage.out ./...

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: coverage.out
          flags: go
          token: ${{ secrets.CODECOV_TOKEN }}

  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    if: github.event_name != 'push' || startsWith(github.ref, 'refs/tags/v')
    outputs:
      # For version tags, always treat as code change to run full pipeline
      code: ${{ startsWith(github.ref, 'refs/tags/v') || steps.filter.outputs.code }}
      tests: ${{ steps.filter.outputs.tests }}
      e2e: ${{ steps.filter.outputs.e2e }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Check for code changes
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            code:
              - '**/*.go'
              - '!**/*_test.go'
              - 'go.mod'
              - 'go.sum'
              - 'web/**'
              - 'migrations/**'
              - 'Containerfile'
              - 'compose*.yaml'
              - '.github/workflows/**'
              - 'Makefile'
              - 'scripts/**'
              - '.golangci.yaml'
            tests:
              - '**/*_test.go'
            e2e:
              - 'tests/e2e/**'
              - 'playwright.config.js'
              - 'Containerfile.playwright'
              - 'e2e/**'
              - 'scripts/e2e.sh'

  secrets:
    name: Secret Scan
    runs-on: ubuntu-latest
    if: github.event_name != 'push' || startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  lint:
    name: Lint
    runs-on: ubuntu-latest
    needs: [changes]
    if: needs.changes.outputs.code == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.6.2

  test-go:
    name: Go Tests
    runs-on: ubuntu-latest
    needs: [changes]
    if: needs.changes.outputs.code == 'true' || needs.changes.outputs.tests == 'true' || needs.changes.outputs.e2e == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Download dependencies
        run: go mod download

      - name: Run tests
        run: go test -v -race -coverprofile=coverage.out ./...

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: coverage.out
          flags: go
          token: ${{ secrets.CODECOV_TOKEN }}

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v6
        with:
          name: go-coverage
          path: coverage.out

  test-js:
    name: JavaScript Tests
    runs-on: ubuntu-latest
    needs: [changes]
    if: needs.changes.outputs.code == 'true' || needs.changes.outputs.tests == 'true' || needs.changes.outputs.e2e == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "20"

      - name: Run JavaScript tests
        run: node web/static/js/tests/runner.js

  e2e:
    name: E2E (${{ matrix.project }})
    runs-on: ubuntu-latest
    needs: [changes]
    if: needs.changes.outputs.code == 'true' || needs.changes.outputs.tests == 'true' || needs.changes.outputs.e2e == 'true'
    strategy:
      fail-fast: false
      matrix:
        project: [firefox, chromium, webkit, mobile-chromium, mobile-webkit]
    env:
      FEATURE_AI_ENABLED: "true"
      AI_STUB: "1"
      PLAYWRIGHT_BASE_URL: "http://app.test:8080"
      APP_BASE_URL: "http://app.test:8080"
      # Shared runners are noisy; single-worker execution is slower but significantly more stable.
      PLAYWRIGHT_WORKERS: "1"
      PLAYWRIGHT_RETRIES: "2"
      PLAYWRIGHT_TEST_TIMEOUT_MS: "90000"
      PLAYWRIGHT_EXPECT_TIMEOUT_MS: "15000"
      # Speed up the background reminder runner in CI so scheduled reminder tests
      # reliably deliver within Playwright's per-test timeout.
      REMINDERS_POLL_INTERVAL: "1s"
      GOOGLE_OAUTH_ENABLED: "true"
      GOOGLE_OAUTH_CLIENT_ID: "oidc-test"
      GOOGLE_OAUTH_CLIENT_SECRET: "oidc-secret"
      GOOGLE_OAUTH_REDIRECT_URL: "http://app.test:8080/api/auth/google/callback"
      GOOGLE_OIDC_ISSUER_URL: "http://oidc:5555"
      GOOGLE_OIDC_SCOPES: "openid,email,profile"
      OAUTH_ALLOWED_PROVIDERS: "google"
      OIDC_ISSUER_URL: "http://oidc:5555"
      OIDC_CLIENT_ID: "oidc-test"
      OIDC_CLIENT_SECRET: "oidc-secret"
      OIDC_REDIRECT_URI: "http://app.test:8080/api/auth/google/callback"
      OIDC_BASE_URL: "http://oidc:5555"
      # Billing E2E: enable billing with mock Stripe
      BILLING_ENABLED: "true"
      STRIPE_SECRET_KEY: "sk_test_mock"
      STRIPE_WEBHOOK_SECRET: "whsec_test"
      STRIPE_API_BASE_URL: "http://stripe-mock:12111"
      STRIPE_MOCK_BASE_URL: "http://stripe-mock:12111"
      STRIPE_PREMIUM_PRICE_MONTHLY: "price_premium_monthly"
      STRIPE_PREMIUM_PRICE_YEARLY: "price_premium_yearly"
      STRIPE_PREMIUM_PRICE_LIFETIME: "price_premium_lifetime"
      STRIPE_TIP_PRICE_5: "price_tip_5"
      STRIPE_TIP_PRICE_10: "price_tip_10"
      STRIPE_TIP_PRICE_20: "price_tip_20"
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Build web assets
        run: ./scripts/build-assets.sh

      - name: Start OIDC mock and Stripe mock
        run: docker compose --profile e2e up -d --build oidc stripe-mock

      - name: Wait for OIDC mock
        run: |
          for i in {1..60}; do
            if curl -fsS http://localhost:5555/.well-known/openid-configuration >/dev/null 2>&1; then
              echo "OIDC mock is healthy."
              exit 0
            fi
            sleep 2
          done
          echo "OIDC mock did not become healthy in time."
          docker compose logs oidc
          exit 1

      - name: Wait for Stripe mock
        run: |
          for i in {1..60}; do
            if curl -fsS http://localhost:12111/health >/dev/null 2>&1; then
              echo "Stripe mock is healthy."
              exit 0
            fi
            sleep 2
          done
          echo "Stripe mock did not become healthy in time."
          docker compose logs stripe-mock
          exit 1

      - name: Start stack
        run: docker compose --profile e2e up -d --build app postgres redis mailpit

      - name: Wait for app health
        run: |
          for i in {1..60}; do
            if curl -fsS http://localhost:8080/health >/dev/null 2>&1; then
              echo "App is healthy."
              exit 0
            fi
            sleep 2
          done
          echo "App did not become healthy in time."
          docker compose logs app
          exit 1

      - name: Seed test data
        run: ./scripts/seed.sh http://localhost:8080

      - name: Run Playwright
        env:
          PLAYWRIGHT_HEADLESS: "true"
          PLAYWRIGHT_BROWSERS: ${{ matrix.project }}
        run: |
          mkdir -p test-results playwright-report
          docker compose --profile e2e run --rm playwright \
            /opt/playwright/node_modules/.bin/playwright test \
            --project="${{ matrix.project }}" \
            --workers="${PLAYWRIGHT_WORKERS}" \
            --retries="${PLAYWRIGHT_RETRIES}"

      - name: Verify app with AI disabled
        env:
          FEATURE_AI_ENABLED: "false"
          PLAYWRIGHT_OUTPUT_DIR: "/test-results/ai-disabled"
          PLAYWRIGHT_REPORT_DIR: "/playwright-report/ai-disabled"
        run: |
          docker compose --profile e2e up -d --no-deps --force-recreate --wait app
          docker compose --profile e2e run --rm playwright \
            /opt/playwright/node_modules/.bin/playwright test ai-disabled.spec.js mobile-flows.spec.js \
            --project="${{ matrix.project }}" \
            --workers="${PLAYWRIGHT_WORKERS}" \
            --retries="${PLAYWRIGHT_RETRIES}"

      - name: Dump container diagnostics on failure
        if: failure()
        run: |
          docker compose --profile e2e ps
          docker compose --profile e2e logs --tail=200 app postgres redis mailpit oidc stripe-mock || true

      - name: Fix Playwright artifact permissions
        if: always()
        run: |
          sudo chown -R "$(id -u)":"$(id -g)" test-results playwright-report || true
          sudo chmod -R a+rX test-results playwright-report || true

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: playwright-report-${{ matrix.project }}
          path: playwright-report

      - name: Upload Playwright results
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: playwright-test-results-${{ matrix.project }}
          path: test-results

      - name: Tear down stack
        if: always()
        run: docker compose down -v

  build-image:
    name: Build Image (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    needs: [changes, secrets, lint, test-go, test-js, e2e]
    if: >
      needs.changes.outputs.code == 'true' &&
      needs.secrets.result == 'success' &&
      needs.lint.result == 'success' &&
      needs.test-go.result == 'success' &&
      needs.test-js.result == 'success' &&
      needs.e2e.result == 'success' &&
      github.actor != 'dependabot[bot]'
    strategy:
      matrix:
        include:
          - arch: amd64
            runner: ubuntu-latest
          - arch: arm64
            runner: ubuntu-24.04-arm
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Quay.io
        if: startsWith(github.ref, 'refs/tags/v')
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.QUAY_USERNAME }}
          password: ${{ secrets.QUAY_PASSWORD }}

      - name: Build image (no push)
        if: "!startsWith(github.ref, 'refs/tags/v')"
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Containerfile
          platforms: linux/${{ matrix.arch }}
          cache-from: type=gha,scope=${{ matrix.arch }}
          cache-to: type=gha,mode=max,scope=${{ matrix.arch }}

      - name: Build and push by digest (tags only)
        if: startsWith(github.ref, 'refs/tags/v')
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Containerfile
          platforms: linux/${{ matrix.arch }}
          outputs: type=image,name=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }},push-by-digest=true,name-canonical=true,push=true
          cache-from: type=gha,scope=${{ matrix.arch }}
          cache-to: type=gha,mode=max,scope=${{ matrix.arch }}

      - name: Export digest
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          mkdir -p /tmp/digests
          digest="${{ steps.build.outputs.digest }}"
          touch "/tmp/digests/${digest#sha256:}"

      - name: Upload digest
        if: startsWith(github.ref, 'refs/tags/v')
        uses: actions/upload-artifact@v6
        with:
          name: digests-${{ matrix.arch }}
          path: /tmp/digests/*
          if-no-files-found: error
          retention-days: 1

  scan-and-push:
    name: Scan, Sign & Push Multi-Arch Manifest
    runs-on: ubuntu-latest
    needs: [build-image]
    if: >
      github.event_name == 'push' &&
      startsWith(github.ref, 'refs/tags/v') &&
      github.actor != 'dependabot[bot]'
    outputs:
      digest: ${{ steps.digest.outputs.digest }}
      tag: ${{ steps.tag.outputs.tag }}
    permissions:
      contents: read
      packages: write
      id-token: write  # Required for keyless signing with Sigstore
    steps:
      - name: Download digests
        uses: actions/download-artifact@v7
        with:
          path: /tmp/digests
          pattern: digests-*
          merge-multiple: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Quay.io
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.QUAY_USERNAME }}
          password: ${{ secrets.QUAY_PASSWORD }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}

      - name: Create and push manifest
        id: manifest
        working-directory: /tmp/digests
        run: |
          docker buildx imagetools create $(jq -cr '.tags | map("-t " + .) | join(" ")' <<< "$DOCKER_METADATA_OUTPUT_JSON") \
            $(printf '${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@sha256:%s ' *)

      - name: Get manifest digest
        id: digest
        run: |
          DIGEST=$(docker buildx imagetools inspect ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest --format '{{json .Manifest.Digest}}' | tr -d '"')
          echo "digest=$DIGEST" >> $GITHUB_OUTPUT
          echo "Manifest digest: $DIGEST"

      - name: Determine image tag
        id: tag
        run: |
          if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            # Strip 'v' prefix for semver tag (v1.0.5 -> 1.0.5)
            TAG="${GITHUB_REF#refs/tags/v}"
          else
            TAG="latest"
          fi
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "Image tag: $TAG"

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          # Pin the Trivy binary: trivy-action@v0.35.0 defaults to v0.69.1, which
          # has no GitHub Release/assets, so the binary download 404s. Pin a real
          # release explicitly (also makes scans reproducible).
          version: v0.71.2
          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          format: "table"
          exit-code: "1"
          ignore-unfixed: true
          vuln-type: "os,library"
          severity: "CRITICAL,HIGH"

      - name: Install Cosign
        uses: sigstore/cosign-installer@v3

      - name: Sign image with Cosign (keyless)
        env:
          COSIGN_EXPERIMENTAL: "true"
        run: |
          # Sign by digest (more secure than signing by tag)
          cosign sign --yes ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.digest.outputs.digest }}

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [scan-and-push]
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Get version
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          body: |
            ## Container Image

            ```bash
            podman pull quay.io/yearofbingo/yearofbingo:${{ steps.version.outputs.version }}
            ```

            **Available tags:**
            - `quay.io/yearofbingo/yearofbingo:${{ steps.version.outputs.version }}` (this release)
            - `quay.io/yearofbingo/yearofbingo:latest` (always latest)

  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [scan-and-push, e2e]
    if: >
      (
        github.event_name == 'workflow_dispatch' &&
        needs.scan-and-push.result == 'success'
      ) ||
      (
        github.event_name == 'push' &&
        startsWith(github.ref, 'refs/tags/v') &&
        needs.scan-and-push.result == 'success' &&
        needs.e2e.result == 'success'
      )
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install cloudflared
        run: |
          CLOUDFLARED_VERSION="2026.1.2"
          CLOUDFLARED_SHA256="e157c54e929cc289cbd53860453168c2fe3439eb55e2e965a56579252585d9c1"
          curl -fsSL "https://github.com/cloudflare/cloudflared/releases/download/${CLOUDFLARED_VERSION}/cloudflared-linux-amd64" -o cloudflared
          echo "${CLOUDFLARED_SHA256}  cloudflared" | sha256sum -c -
          chmod +x cloudflared
          sudo mv cloudflared /usr/local/bin/

      - name: Deploy to server
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          REDIS_PASSWORD: ${{ secrets.REDIS_PASSWORD }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          EMAIL_FROM_ADDRESS: ${{ secrets.EMAIL_FROM_ADDRESS }}
          BILLING_ENABLED: ${{ secrets.BILLING_ENABLED }}
          STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
          STRIPE_WEBHOOK_SECRET: ${{ secrets.STRIPE_WEBHOOK_SECRET }}
          STRIPE_PREMIUM_PRICE_MONTHLY: ${{ secrets.STRIPE_PREMIUM_PRICE_MONTHLY }}
          STRIPE_PREMIUM_PRICE_YEARLY: ${{ secrets.STRIPE_PREMIUM_PRICE_YEARLY }}
          STRIPE_PREMIUM_PRICE_LIFETIME: ${{ secrets.STRIPE_PREMIUM_PRICE_LIFETIME }}
          STRIPE_TIP_PRICE_5: ${{ secrets.STRIPE_TIP_PRICE_5 }}
          STRIPE_TIP_PRICE_10: ${{ secrets.STRIPE_TIP_PRICE_10 }}
          STRIPE_TIP_PRICE_20: ${{ secrets.STRIPE_TIP_PRICE_20 }}
          BACKUP_NOTIFY_EMAILS: ${{ secrets.BACKUP_NOTIFY_EMAILS }}
          BACKUP_ENCRYPTION_KEY: ${{ secrets.BACKUP_ENCRYPTION_KEY }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          IMAGE_DIGEST: ${{ needs.scan-and-push.outputs.digest }}
          IMAGE_TAG: ${{ needs.scan-and-push.outputs.tag }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GOOGLE_OAUTH_ENABLED: ${{ secrets.GOOGLE_OAUTH_ENABLED }}
          GOOGLE_OAUTH_CLIENT_ID: ${{ secrets.GOOGLE_OAUTH_CLIENT_ID }}
          GOOGLE_OAUTH_CLIENT_SECRET: ${{ secrets.GOOGLE_OAUTH_CLIENT_SECRET }}
          GOOGLE_OAUTH_REDIRECT_URL: ${{ secrets.GOOGLE_OAUTH_REDIRECT_URL }}
          GOOGLE_OIDC_ISSUER_URL: ${{ secrets.GOOGLE_OIDC_ISSUER_URL }}
          GOOGLE_OIDC_SCOPES: ${{ secrets.GOOGLE_OIDC_SCOPES }}
          OAUTH_ALLOWED_PROVIDERS: ${{ secrets.OAUTH_ALLOWED_PROVIDERS }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519

          # Configure SSH to use Cloudflare Tunnel
          cat >> ~/.ssh/config << 'SSHCONFIG'
          Host ssh.yearofbingo.com
            User deploy
            ProxyCommand cloudflared access ssh --hostname %h
            StrictHostKeyChecking accept-new
          SSHCONFIG

          # Update compose file with image digest (or fallback to :latest)
          if [ -n "${IMAGE_DIGEST}" ]; then
            # Use digest with tag as comment
            sed "s|image: quay.io/yearofbingo/yearofbingo:latest|# tag: ${IMAGE_TAG}\n    image: quay.io/yearofbingo/yearofbingo@${IMAGE_DIGEST}|" \
              compose.server.yaml > compose.server.deploy.yaml
          else
            # Fallback to :latest for deploy_only workflow dispatch
            cp compose.server.yaml compose.server.deploy.yaml
          fi

          # Copy compose file to server via tunnel
          scp compose.server.deploy.yaml ssh.yearofbingo.com:/opt/yearofbingo/compose.yaml
          scp scripts/*.sh ssh.yearofbingo.com:/opt/yearofbingo/scripts/

          # Deploy via tunnel
          ssh ssh.yearofbingo.com << EOF
            cd /opt/yearofbingo

            # Create/update .env file with secrets
            cat > .env << 'ENVFILE'
          DB_PASSWORD=${DB_PASSWORD}
          REDIS_PASSWORD=${REDIS_PASSWORD}
          RESEND_API_KEY=${RESEND_API_KEY}
          EMAIL_FROM_ADDRESS=${EMAIL_FROM_ADDRESS}
          BACKUP_NOTIFY_EMAILS=${BACKUP_NOTIFY_EMAILS}
          GEMINI_API_KEY=${GEMINI_API_KEY}
          BACKUP_ENCRYPTION_KEY=${BACKUP_ENCRYPTION_KEY}
          GOOGLE_OAUTH_ENABLED=${GOOGLE_OAUTH_ENABLED}
          GOOGLE_OAUTH_CLIENT_ID=${GOOGLE_OAUTH_CLIENT_ID}
          GOOGLE_OAUTH_CLIENT_SECRET=${GOOGLE_OAUTH_CLIENT_SECRET}
          GOOGLE_OAUTH_REDIRECT_URL=${GOOGLE_OAUTH_REDIRECT_URL}
          GOOGLE_OIDC_ISSUER_URL=${GOOGLE_OIDC_ISSUER_URL}
          GOOGLE_OIDC_SCOPES=${GOOGLE_OIDC_SCOPES}
          OAUTH_ALLOWED_PROVIDERS=${OAUTH_ALLOWED_PROVIDERS}
          BILLING_ENABLED=${BILLING_ENABLED}
          STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
          STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
          STRIPE_PREMIUM_PRICE_MONTHLY=${STRIPE_PREMIUM_PRICE_MONTHLY}
          STRIPE_PREMIUM_PRICE_YEARLY=${STRIPE_PREMIUM_PRICE_YEARLY}
          STRIPE_PREMIUM_PRICE_LIFETIME=${STRIPE_PREMIUM_PRICE_LIFETIME}
          STRIPE_TIP_PRICE_5=${STRIPE_TIP_PRICE_5}
          STRIPE_TIP_PRICE_10=${STRIPE_TIP_PRICE_10}
          STRIPE_TIP_PRICE_20=${STRIPE_TIP_PRICE_20}
          ENVFILE
            chmod 600 .env
            chmod 0755 scripts/*.sh

            # Create/update rclone config for R2 backups
            mkdir -p ~/.config/rclone
            cat > ~/.config/rclone/rclone.conf << 'RCLONECONF'
          [r2]
          type = s3
          provider = Cloudflare
          access_key_id = ${R2_ACCESS_KEY_ID}
          secret_access_key = ${R2_SECRET_ACCESS_KEY}
          endpoint = https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com
          acl = private
          no_check_bucket = true
          RCLONECONF
            chmod 600 ~/.config/rclone/rclone.conf

            # Pull and restart
            podman-compose pull
            podman-compose down || true
            podman-compose up -d
          EOF
